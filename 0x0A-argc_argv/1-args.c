#include <stdio.h>

/**
 * main - prints the number of arguments passed to it
 * @argc: parameter
 * @argv: argument vector
 * 
 * Return: 0
 */

int main(int argc, char *argv[])
{
    printf("%d", argc);
    return (0);
}