#include "main.h"

/**
 * _isupper: checks for uppercase character
 * @c: character to be checked
 * Return: 1 if c is uppercase
 * 0 otherwise
 */

int _isupper(int c)
{
	int retval;

	if (_isupper(c))
	{
		retval = 1;
	}
	else
	{
		retval = 0;
	}
	return (retval);
}

