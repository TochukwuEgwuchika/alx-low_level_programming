#include "main.h"

/**
 * _islower - checks for a lowercase character
 * @c: character to check case
 * Return: 1 if c is lowercase
 * Otherwise: 0
 */

int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
